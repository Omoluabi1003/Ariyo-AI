# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Often committed, but for this exercise, let's ignore to be safe if it causes issues with the sandbox file limits.
                  # In a real project, package-lock.json or yarn.lock should be committed.

# Vite
dist/
.vite/
*.local

# OS generated files
.DS_Store
Thumbs.db
